# P6C Slice 12 — EMPLOYEE Party Display Name UPDATE

**VERIFIED COMPLETE; AUTHENTICATED BROWSER ACCEPTANCE COMPLETE; FIXTURE CLEANUP COMPLETE.** Development project `eqnzueginpkskbnqvgoc`. P6C IN PROGRESS; P6D/P6E NOT STARTED; production readiness DEFERRED.

The disposable Development identity `p6c-s12-browser@example.test` was provisioned separately for the authorized acceptance run; no password/token or UUID is stored in this repository. `preflight.sql` passed before setup. The guarded fixture was created once, acceptance completed, authority restored, the operator signed out, `browser-cleanup.sql` completed, and `verify.sql` returned all Slice 12 fixture/Auth/profile counts 0 with global Companies/settings 14/14 and missing/orphan 0/0.

## Authenticated acceptance matrix — COMPLETE

1. Sign in through the real Development browser Auth form, select Alpha, open `/parties`. ACCOUNTING_ADMIN can rename ACTIVE and INACTIVE EMPLOYEE rows. Form is populated with the selected name and has only `name`; save trims boundary whitespace (including Unicode), preserves internal spaces/case, accepts Arabic and exactly 200 Unicode characters, rejects empty/whitespace and 201 characters. Verify code, type, status, leading-zero TRN, contact, notes and creation provenance unchanged; no EMPLOYEE create/delete/status/configuration controls.
2. Two real tabs load the same EMPLOYEE. Save in A and submit stale B: B gets conflict from zero affected rows; winner survives with no overwrite or silent retry. Refresh/review allows a fresh edit.
3. Switch Alpha/Beta and rename Beta independently. No Alpha row, draft, feedback or branding leaks into Beta. Switch back and confirm Alpha state.
4. Use `browser-role.sql` execution copies for Alpha only. ACCOUNTING_ADMIN edit allowed; ACCOUNTANT and MANAGEMENT_VIEWER read EMPLOYEE without edit; PROCUREMENT and DATA_ENTRY see only their permitted non-sensitive Party types and no EMPLOYEE; PROJECT_MANAGER and SYSTEM_ADMIN have no Party rows/edit. Downgrade while the EMPLOYEE form is open and use genuine tab blur/focus: stale form disappears, authority refetches and role display changes without relying on reload.
5. Use `browser-authority.sql` execution copies to revoke/restore Alpha Company, Alpha membership and profile separately. Tab-return/refetch must fail closed. Alpha-only membership revocation leaves Beta available; profile revocation removes all Company access. Restore all before continuing.
6. Test ordinary tab-return preserves an unsaved draft, keyboard Enter/open/save/cancel, focus restoration and error focus. At 390px test EN/AR/RTL and a full 200-character unbroken ASCII/Arabic EMPLOYEE name: visible readable wrapping and no document/body horizontal overflow.
7. Type boundary: Alpha Supplier has its existing Supplier edit/status controls, Alpha OTHER has only its existing name editor, OWNER/CUSTODIAN/SUBCONTRACTOR have no new editor. The EMPLOYEE form cannot mutate those rows or protected columns. Check Supplier display regression read-only; its name/status must remain at fixture values for the guarded cleanup. Financial/P6D routes remain unwritable holding states.
8. Sign out and close all acceptance sessions. Restore Alpha role/Company/membership/profile to ACCOUNTING_ADMIN/ACTIVE. Run the exact guarded `browser-cleanup.sql` with the fixture UUID in a private execution copy. Then run `verify.sql`: all Slice 12 fixture Companies/Parties/memberships/settings/assignments and browser Auth/profile counts **0**; global Companies/settings **14/14**, missing/orphan **0/0**. Preserve the fixture on any failure and report the exact failed step; never loosen cleanup guards.

## Fixture manifest

Prefix `82200000-0000-4000-8000-0000000000`: `a1` Alpha Company/settings/membership; `a2` Beta; `b1` Alpha INACTIVE EMPLOYEE with leading-zero TRN, Arabic name/contact and fixed notes; `b2` Alpha ACTIVE EMPLOYEE; `b3` Beta ACTIVE EMPLOYEE; `b4` Alpha Supplier regression; `b5` Alpha OTHER regression; `b6` OWNER; `b7` CUSTODIAN; `b8` SUBCONTRACTOR. No Project, GL, subcontract contract or financial document fixture. Role/authority helpers affect only the exact Alpha fixture or disposable profile.

Automated implementation evidence is in [the phase record](../../P6C_SLICE_12_EMPLOYEE_PARTY_NAME.md). The isolated Chromium fixture remains automated in-memory evidence rather than hosted acceptance. Authenticated hosted acceptance was subsequently completed manually against Development. The guarded setup/helpers were used only for their exact synthetic fixture; authority was restored before cleanup. Cleanup and final integrity verification completed successfully.
