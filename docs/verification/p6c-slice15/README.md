# P6C Slice 15 — SUBCONTRACTOR Party Display Name UPDATE

**VERIFIED COMPLETE; AUTHENTICATED BROWSER ACCEPTANCE COMPLETE; FIXTURE CLEANUP COMPLETE.** Development project `eqnzueginpkskbnqvgoc`. P6C IN PROGRESS; P6D/P6E NOT STARTED; Production readiness DEFERRED.

For later authorized acceptance, provision only a disposable synthetic Development Auth user `p6c-s15-browser@example.test` with an ACTIVE profile. The SQL has an empty UUID placeholder and no password/token. Replace **only** `makeracc.fixture_user` in private execution copies; do not commit copies. Run read-only `preflight.sql` first: migration `20260922150000`, exact Parties catalog, and global Companies/settings 14/14 with missing/orphan 0/0 are prerequisites. Execute guarded `browser-setup.sql` once only. The helpers set only the exact Alpha role, Company, membership or profile. Do not run cleanup until all acceptance passes or an authorized abort requires it.

## Authenticated acceptance matrix — COMPLETE

1. Sign in through real Development Auth, select Alpha, open `/parties`. ACCOUNTING_ADMIN and PROCUREMENT can rename ACTIVE and INACTIVE SUBCONTRACTOR rows. The populated form contains only `name`. Save trims Unicode boundary whitespace, preserves internal spaces/case, accepts Arabic and exactly 200 Unicode characters, rejects empty/whitespace and 201 characters. Verify code, type, status, leading-zero TRN, contact, notes and creation provenance unchanged. No SUBCONTRACTOR create/delete/status/contract/financial controls.
2. Open the same SUBCONTRACTOR in two real tabs, B first. Save in A, then submit stale B: B reports conflict from zero affected rows; A's result survives, with no overwrite or silent retry. Refresh/review permits a new edit.
3. Switch Alpha/Beta and rename Beta independently. No Alpha row, draft, feedback or branding leaks into Beta. Switch back and confirm Alpha state.
4. Apply `browser-role.sql` execution copies to Alpha only. ACCOUNTING_ADMIN and PROCUREMENT edit. ACCOUNTANT, MANAGEMENT_VIEWER and DATA_ENTRY read SUBCONTRACTOR without edit. PROJECT_MANAGER and SYSTEM_ADMIN see no Party rows/edit. Downgrade while form is open; genuine browser tab blur/focus must revalidate authority and remove stale controls without relying on page reload.
5. Apply `browser-authority.sql` execution copies to revoke/restore Alpha Company, Alpha membership and profile separately. Tab-return/refetch must fail closed. Alpha-only membership revocation leaves Beta available; profile revocation removes all Company access. Restore all authority before continuing.
6. Verify ordinary tab-return preserves an unsaved draft, keyboard Enter/open/save/cancel, focus restoration and validation/error focus. At 390px test EN/AR/RTL and full 200-character unbroken ASCII/Arabic SUBCONTRACTOR names: readable wrapping, no document/body horizontal overflow.
7. On `/subcontracts`, the exact fixture Subcontract `0001` displays the current renamed SUBCONTRACTOR name from scoped Parties, without changing the Subcontract's ID, number, scope, zero value, retention, status or other fields. Revisit after rename and tenant switch; no cross-tenant display leakage. The Subcontract page is read-only. Supplier edit/status and OTHER, EMPLOYEE, CUSTODIAN and OWNER name controls retain their prior permissions. No advance/certificate/payment/retention/journal or P6D route becomes writable.
8. Restore Alpha role/Company/membership/profile to ACCOUNTING_ADMIN/ACTIVE. Sign out and close sessions. Run exact guarded `browser-cleanup.sql` with the fixture UUID in a private execution copy, then `verify.sql`. Require all Slice 15 fixture Companies/Parties/Projects/Subcontracts/memberships/settings/assignments and browser Auth/profile counts **0**; global Companies/settings **14/14**, missing/orphan **0/0**. On any failure preserve evidence and report the exact step; never loosen cleanup guards.

## Fixture manifest

Prefix `85200000-0000-4000-8000-0000000000`: `a1` Alpha Company/settings/membership; `a2` Beta; `b1` Alpha INACTIVE SUBCONTRACTOR with leading-zero TRN, Arabic name/contact and fixed notes; `b2` Alpha ACTIVE SUBCONTRACTOR; `b3` Beta ACTIVE SUBCONTRACTOR; `b4` Supplier; `b5` OTHER; `b6` EMPLOYEE; `b7` CUSTODIAN; `b8` OWNER regression; `c1` Alpha Project; `d1` Alpha read-only Subcontract linked to `b2`, with zero required value and retention. No posted financial document, GL, Treasury, advance, certificate or payment fixture. Role/authority helpers affect only exact Alpha fixture or disposable profile.

Automated evidence is in [the phase record](../../P6C_SLICE_15_SUBCONTRACTOR_PARTY_NAME.md). Authenticated Development browser acceptance was subsequently completed manually. A cleanup-kit guard defect was found during closure: setup leaves the minimal Project at schema-default PLANNING while the prepared cleanup expected ACTIVE. The failed transaction rolled back; `browser-cleanup.sql` was corrected to PLANNING and exact guarded cleanup plus final verification PASSed. All Slice 15 fixture/Auth/profile counts are 0; global Companies/settings are 14/14 with missing/orphan 0/0.
