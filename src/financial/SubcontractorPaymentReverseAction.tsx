import { useEffect, useRef, useState, type FormEvent } from "react";
import { useT } from "../i18n/I18nContext";
import { getSupabaseClient } from "../lib/supabase";
import { loadSubcontractorPaymentReverseAttempt, saveSubcontractorPaymentReverseAttempt, sendSubcontractorPaymentReverseAttempt, subcontractorPaymentReverseStorageKey, type SubcontractorPaymentReverseAttempt } from "./subcontractorPaymentReverseAttempt";
import { canReverseSubcontractorPayment, normalizeSubcontractorPaymentReverseInput } from "./subcontractorPaymentReverseRepository";
import { readSubcontractorPaymentById, type SubcontractorPaymentRead } from "./subcontractorPaymentRepository";

export function SubcontractorPaymentReverseAction({ userId, companyId, role, payment, onRefresh }: { userId: string; companyId: string; role: string; payment: SubcontractorPaymentRead; onRefresh: () => void }) {
  const t = useT(); const [initial] = useState(() => { try { return { attempt: loadSubcontractorPaymentReverseAttempt(window.sessionStorage, userId, companyId, payment.id), ready: true }; } catch { return { attempt: null, ready: false }; } }); const [open, setOpen] = useState(Boolean(initial.attempt));
  const [attempt, setAttempt] = useState<SubcontractorPaymentReverseAttempt | null>(initial.attempt); const [ready, setReady] = useState(initial.ready); const [busy, setBusy] = useState(false); const [confirmed, setConfirmed] = useState<SubcontractorPaymentRead | null>(null);
  const [message, setMessage] = useState<"invalid" | "unresolved" | "rejected" | "readError" | "storageError" | "denied" | null>(initial.ready ? null : "storageError"); const generation = useRef(0); const lock = useRef(false);
  useEffect(() => { const { data } = getSupabaseClient().auth.onAuthStateChange((event, session) => { if (event === "SIGNED_OUT" || !session || session.user.id !== userId) { generation.current++; setReady(false); setAttempt(null); setConfirmed(null); setMessage("denied"); } }); const invalidate = () => { generation.current++; }; return () => { invalidate(); data.subscription.unsubscribe(); }; }, [userId, companyId, payment.id]);
  const recover = async (pending: SubcontractorPaymentReverseAttempt, firstSend = false) => { if (lock.current || !ready || !(role === "ACCOUNTING_ADMIN" && (pending.receipt || canReverseSubcontractorPayment(role, payment.status)))) return; lock.current = true; setBusy(true); setMessage(null); const version = generation.current; const current = () => generation.current === version; let result = pending;
    try { result = await sendSubcontractorPaymentReverseAttempt(getSupabaseClient(), window.sessionStorage, pending, firstSend); if (!current()) return; setAttempt(result); const row = await readSubcontractorPaymentById(getSupabaseClient(), companyId, result.receipt!.subcontractor_payment_id); const live = await getSupabaseClient().auth.getSession(); if (!current()) return; if (live.error || live.data.session?.user.id !== userId) { setReady(false); setAttempt(null); setMessage("denied"); return; } if (row.status !== "REVERSED" || row.reversal_journal_entry_id !== result.receipt!.reversal_journal_entry_id) throw new Error("readback"); setConfirmed(row); if (payment.status === "POSTED") onRefresh();
    } catch (error) { if (current()) { if (firstSend && error instanceof Error && error.message === "rejected") { setAttempt(null); setMessage("rejected"); } else { setAttempt(result); setMessage(result.receipt ? "readError" : "unresolved"); } } } finally { if (current()) { lock.current = false; setBusy(false); } } };
  const submit = (event: FormEvent<HTMLFormElement>) => { event.preventDefault(); if (attempt || busy || !ready) return; const data = new FormData(event.currentTarget); try { if (String(data.get("confirm")) !== "yes") throw new Error("invalid"); const input = normalizeSubcontractorPaymentReverseInput({ date: String(data.get("date") ?? ""), reason: String(data.get("reason") ?? "") }); const pending: SubcontractorPaymentReverseAttempt = { version: 1, userId, companyId, paymentId: payment.id, key: crypto.randomUUID(), input }; saveSubcontractorPaymentReverseAttempt(window.sessionStorage, pending); setAttempt(pending); void recover(pending, true); } catch (error) { setMessage(error instanceof Error && error.message === "invalid" ? "invalid" : "storageError"); } };
  if (!canReverseSubcontractorPayment(role, payment.status) && !attempt) return null; const button = "rounded-lg border border-slate-300 px-3 py-2 disabled:opacity-50";
  return <div className="mt-3">{!open ? <button type="button" className={button} onClick={() => setOpen(true)}>{t("subcontractorPaymentReverse.open")}</button> : <section className="rounded-lg border border-amber-300 p-4" aria-label={t("subcontractorPaymentReverse.title")}><h4 className="font-semibold">{t("subcontractorPaymentReverse.title")}</h4><p className="mt-2 text-sm">{t("subcontractorPaymentReverse.warning")}</p>{message && <p role="alert" className="mt-2 text-red-800">{t(`subcontractorPaymentReverse.${message}`)}</p>}{busy && <p role="status">{t("subcontractorPaymentReverse.pending")}</p>}
    {attempt ? <div className="mt-3 space-y-2"><p>{t(attempt.receipt ? "subcontractorPaymentReverse.confirmed" : "subcontractorPaymentReverse.frozen")}</p><p>{t("subcontractorPaymentReverse.request")}: <bdi>{attempt.key}</bdi></p>{attempt.receipt && <p>{t("subcontractorPaymentReverse.journal")}: <bdi>{attempt.receipt.reversal_journal_entry_id}</bdi></p>}{confirmed && <p role="status"><bdi>{confirmed.payment_reference} · {t(`subcontractorPaymentRead.${confirmed.status}`)}</bdi></p>}<button type="button" className={button} disabled={busy || !ready} onClick={() => void recover(attempt)}>{t(attempt.receipt ? "subcontractorPaymentReverse.readAgain" : "subcontractorPaymentReverse.retry")}</button>{attempt.receipt && <button type="button" className={button} disabled={busy || !confirmed} onClick={() => { try { window.sessionStorage.removeItem(subcontractorPaymentReverseStorageKey(userId, companyId, payment.id)); setAttempt(null); setConfirmed(null); setMessage(null); setOpen(false); } catch { setMessage("storageError"); } }}>{t("subcontractorPaymentReverse.dismiss")}</button>}</div>
    : <form onSubmit={submit} className="mt-3 space-y-3"><label className="block">{t("subcontractorPaymentReverse.date")}<input name="date" type="date" required className={button} /></label><label className="block">{t("subcontractorPaymentReverse.reason")}<textarea name="reason" required maxLength={1000} className="block w-full rounded-lg border p-2" /></label><label className="flex gap-2"><input name="confirm" value="yes" type="checkbox" required />{t("subcontractorPaymentReverse.confirmCheck")}</label><div className="flex gap-2"><button type="submit" className={button}>{t("subcontractorPaymentReverse.confirm")}</button><button type="button" className={button} onClick={() => { setOpen(false); setMessage(null); }}>{t("subcontractorPaymentReverse.cancel")}</button></div></form>}</section>}</div>;
}
